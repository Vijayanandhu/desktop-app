<Window x:Class="EnterpriseWorkReport.Views.Dialogs.ViewMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="View Message" Width="600" Height="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource AppBgBrush}"
        MinWidth="500" MinHeight="400">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" x:Name="SubjectText" Text="Message Subject" 
                   FontSize="20" FontWeight="Bold" 
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"
                   TextWrapping="Wrap"/>

        <!-- From/To -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="FROM" Style="{StaticResource FieldLabel}"/>
                <TextBlock x:Name="FromText" Text="" FontSize="14" 
                           Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="TO" Style="{StaticResource FieldLabel}"/>
                <TextBlock x:Name="ToText" Text="" FontSize="14" 
                           Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
        </Grid>

        <!-- Date -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="DATE" Style="{StaticResource FieldLabel}"/>
            <TextBlock x:Name="DateText" Text="" FontSize="13" 
                       Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Message Content -->
        <Border Grid.Row="3" Background="#f8f9fa" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ContentText" Text="" TextWrapping="Wrap" 
                           FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>
            </ScrollViewer>
        </Border>

        <!-- Attachment -->
        <Border Grid.Row="4" x:Name="AttachmentPanel" Visibility="Collapsed" 
                Background="#e8f4fd" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="ATTACHMENT" Style="{StaticResource FieldLabel}"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock x:Name="AttachmentName" Text="" FontSize="13" 
                               Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                    <Button Content="📥 Download" Style="{StaticResource PrimaryButton}"
                            Padding="12,6" FontSize="12" Margin="16,0,0,0" Click="Download_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Close Button -->
        <Button Grid.Row="5" Content="Close" Style="{StaticResource PrimaryButton}"
                HorizontalAlignment="Right" Padding="24,10" Click="Close_Click"/>
    </Grid>
</Window>
