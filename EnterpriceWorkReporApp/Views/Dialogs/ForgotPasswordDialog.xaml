<Window x:Class="EnterpriseWorkReport.Views.Dialogs.ForgotPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Forgot Password" Width="450" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource AppBgBrush}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="🔐 Reset Password" 
                   FontSize="22" FontWeight="Bold" 
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
        
        <TextBlock Grid.Row="1" Text="Enter your username to reset your password. Contact admin for assistance."
                   TextWrapping="Wrap" FontSize="13" 
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,24"/>

        <!-- Username -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="USERNAME" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="UsernameBox" Style="{StaticResource ModernTextBox}"/>
        </StackPanel>

        <!-- New Password (Admin only) -->
        <StackPanel Grid.Row="3" x:Name="NewPasswordPanel" Margin="0,0,0,16" Visibility="Collapsed">
            <TextBlock Text="NEW PASSWORD" Style="{StaticResource FieldLabel}"/>
            <PasswordBox x:Name="NewPasswordBox" Style="{StaticResource ModernPasswordBox}"/>
            <TextBlock Text="Enter admin username to reset password" 
                       FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Message -->
        <TextBlock x:Name="MessageText" Grid.Row="4" Text="" 
                   TextWrapping="Wrap" FontSize="13" Margin="0,0,0,16"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource OutlineButton}"
                    Padding="16,10" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Reset Password" Style="{StaticResource PrimaryButton}"
                    Padding="16,10" Click="Reset_Click"/>
        </StackPanel>
    </Grid>
</Window>
