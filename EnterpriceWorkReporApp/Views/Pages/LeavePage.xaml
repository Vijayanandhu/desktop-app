<Page x:Class="EnterpriseWorkReport.Views.Pages.LeavePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource AppBgBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Leave Management" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Employees apply for leave. Admins approve or reject requests."
                       Style="{StaticResource PageSubtitle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="+ Apply Leave" Style="{StaticResource PrimaryButton}"
                            Padding="14,9" FontSize="13" Margin="0,0,10,0" Click="ApplyLeave_Click"/>
                    
                    <!-- Filter Controls -->
                    <TextBox x:Name="FilterEmployeeName" Style="{StaticResource ModernTextBox}" Width="150" Margin="0,0,10,0"
                             Tag="Employee Name" ToolTip="Employee Name" TextChanged="Filter_Changed" />
                    <ComboBox x:Name="FilterStatus" Style="{StaticResource ModernComboBox}"
                               Width="140" Margin="0,0,10,0" SelectionChanged="Filter_Changed">
                        <ComboBoxItem Content="All Statuses" IsSelected="True"/>
                        <ComboBoxItem Content="Pending"/>
                        <ComboBoxItem Content="Approved"/>
                        <ComboBoxItem Content="Rejected"/>
                    </ComboBox>
                    <DatePicker x:Name="FilterDateFrom" Width="130" Margin="0,0,10,0"
                                SelectedDateChanged="Filter_Changed"/>
                    <DatePicker x:Name="FilterDateTo" Width="130" Margin="0,0,10,0"
                                SelectedDateChanged="Filter_Changed"/>
                    <Button Content="Clear" Style="{StaticResource OutlineButton}" Padding="12,9" FontSize="13"
                            Click="ClearFilter_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid x:Name="LeaveGrid" Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="EmployeeCol" Header="Employee" Binding="{Binding EmployeeName}" Width="*"/>
                    <DataGridTextColumn Header="Leave Type" Binding="{Binding LeaveType}" Width="*"/>
                    <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat='dd/MM/yyyy'}" Width="120"/>
                    <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat='dd/MM/yyyy'}" Width="120"/>
                    <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="2*"/>
                    <DataGridTemplateColumn Header="Status" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" FontWeight="SemiBold" FontSize="12"
                                           VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn x:Name="ActionsCol" Header="Actions" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="ApproveBtn" Content="✓" Tag="{Binding Id}" Style="{StaticResource SuccessButton}"
                                            Padding="8,4" FontSize="11" Margin="0,0,4,0" Click="Approve_Click" ToolTip="Approve"/>
                                    <Button x:Name="RejectBtn" Content="✕" Tag="{Binding Id}" Style="{StaticResource DangerButton}"
                                            Padding="8,4" FontSize="11" Margin="0,0,4,0" Click="Reject_Click" ToolTip="Reject"/>
                                    <Button Content="🗑" Tag="{Binding Id}" Style="{StaticResource WarningButton}"
                                            Padding="8,4" FontSize="11" Click="Delete_Click" ToolTip="Delete"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
