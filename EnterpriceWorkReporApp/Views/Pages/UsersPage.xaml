<Page x:Class="EnterpriseWorkReport.Views.Pages.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource AppBgBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="User Management" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Create, edit, and manage employee and admin accounts."
                       Style="{StaticResource PageSubtitle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="+ New User" Style="{StaticResource PrimaryButton}"
                            Padding="16,9" FontSize="13" Margin="0,0,10,0" Click="AddUser_Click"/>
                    <ComboBox x:Name="FilterRole" Style="{StaticResource ModernComboBox}"
                              Width="180" SelectionChanged="Filter_Changed">
                        <ComboBoxItem Content="All Roles"/>
                        <ComboBoxItem Content="Administrator"/>
                        <ComboBoxItem Content="Employee"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBox x:Name="SearchBox" Style="{StaticResource ModernTextBox}"
                             Width="220" Padding="10,8" TextChanged="Search_Changed"/>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="-30,0,12,0" FontSize="14"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid x:Name="UsersGrid" Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTemplateColumn Header="Full Name" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="{Binding FullName}" Tag="{Binding}" 
                                        Style="{StaticResource LinkButton}" 
                                        Click="ViewUserDetails_Click" Cursor="Hand"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="140"/>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource BadgeSuccess}" HorizontalAlignment="Left"
                                        Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Active" FontSize="11" Foreground="#065F46" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Actions" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Tag="{Binding Id}" Style="{StaticResource PrimaryButton}"
                                            Padding="10,5" FontSize="12" Margin="0,0,6,0" Click="EditUser_Click"/>
                                    <Button Content="Reset Pwd" Tag="{Binding Id}" Style="{StaticResource OutlineButton}"
                                            Padding="10,5" FontSize="12" Margin="0,0,6,0" Click="ResetPassword_Click"/>
                                    <Button Content="🗑" Tag="{Binding Id}" Style="{StaticResource DangerButton}"
                                            Padding="10,5" FontSize="12" Click="DeactivateUser_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
